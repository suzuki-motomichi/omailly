/ #bg v-bind:style=("{ backgroundColor: color}")
/ ul.leaf
  li
  li
  li
  li
  li
  li
  li
  li
  li
.snow-fall
  .snow-1●
  .snow-2●
  .snow-3●
  .snow-4●
  .snow-5●
  .snow-6●
  .snow-7●
  .snow-8●
  .snow-9●
  .snow-10●


.container
  .ihai
    = image_pack_tag "winter-top.png", class: 'img-fluid'

  .d-flex.justify-content-center
    .btn-border-gradient-wrap.btn-border-gradient-wrap--gold
      = link_to new_post_path, class:'btn btn-border-gradient' do
        span.btn-text-gradient--gold #{t('.new')}

    .btn-border-gradient-wrap-1.btn-border-gradient-wrap--silver
      = link_to posts_path, class: 'btn btn-border-gradient t_1' do
        span.btn-text-gradient--silver #{t('.index')}

h1.m-5 - 厳選された参拝 -

table.table
  thaed.td-default
    tr.tr-tl
      th.rank= Post.human_attribute_name(:rank)
      th.name= Post.human_attribute_name(:title)
      th.content= Post.human_attribute_name(:content)
      th.yaer= Post.human_attribute_name(:last_day)
    tbody.t-body
      = render partial: "ranking", collection: @posts, as: :post

/ = javascript_pack_tag 'hello_vue'
