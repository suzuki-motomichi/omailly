<% if @pagy.page == 1 %>
    $('#records_table').html("<%= j(render @posts, post: @post)%>");
<% else %>
    $('#records_table').append("<%= j(render @posts, post: @post)%>");
<% end %>
$('#div_next_link').html("<%= j(render 'shared/next_link') %>");
$("#pageScroll").attr('data-page', "<%= @next_page %>");
